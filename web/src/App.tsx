/*
- What it does:
  Exports the dashboard shell through the Vite app entry convention.
- Inputs:
  The AppShell default export.
- Outputs:
  The React component consumed by main.tsx.
*/

export { default } from './AppShell'
